{
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  },
  "scripts": {
    "sync": "bash bin/shell_cmd.sh",
    "docs:dev": "pnpm run sync && vitepress dev docs",
    "docs:build": "pnpm run sync && NODE_OPTIONS='--max-old-space-size=8192' vitepress build docs",
    "docs:preview": "vitepress preview docs"
  }
}
